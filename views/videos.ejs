<%- include('partials/head') %>
<%- include('partials/header') %>

<main class="container">
  <div class="page-head">
    <h1>All Videos</h1>
    <p><%= total %> videos</p>
  </div>
  <% if (videos.length === 0) { %>
    <p class="empty-note">No videos yet — check back soon! 🎬</p>
  <% } %>
  <div class="video-grid">
    <% videos.forEach(function(v, i){ %>
      <%- include('partials/video-card', {v: v}) %>
      <% if (i === 5 && ads.native) { %><%- include('partials/ad', {slot:'native', code: ads.native}) %><% } %>
    <% }); %>
  </div>
  <% if (totalPages > 1) { %>
  <nav class="pagination" aria-label="Pages">
    <% if (page > 1) { %><a href="/videos?page=<%= page-1 %>">← Prev</a><% } %>
    <span>Page <%= page %> of <%= totalPages %></span>
    <% if (page < totalPages) { %><a href="/videos?page=<%= page+1 %>">Next →</a><% } %>
  </nav>
  <% } %>
</main>

<%- include('partials/footer') %>
